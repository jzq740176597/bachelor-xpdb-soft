--4-1
1. pipe sdf easily penetrate when move
2. possible use generated soft-asset with diff-uniform scaled to create Soft? 

--3-22
1. small-sphere => hang attach but jittering (with damp = 1)
2. xpbd-colliderSource => sphereBoxCld Still penetrate even tunnelling when move fast! (ref : PhysX imp ,utilise unity MovePostion()?)

--3-12
1. static cld : remove update logic
2. kinematic cld pass through / tunnel case <See Squash case> ? CCD not working? <ref : rigidbody.MovePosition() API>
3. soft-soft collision not working at all
*
4. surface particle can make static / kinematic CAN connected to SoftAttachJoint<required unity phx rigidbody>: can create joints
	/links between the SoftAttachJoint-rigidbody and particles (the constrains is dist-contrains with complience CONTROL sitffness like existed, default 0)
	Then When The SoftAttachJoint-rigidbody moved Then Contrains will be applied to the particles and make them move together with the rigidbody.
	a. maybe create utility EdiorWindow to visually choose the *particles grps* and later can be used / Ref to create the joints or sth Eg: SoftAttachJoint. of course the *particles grps* data can be store in soft-asset <XXX Type>
-----------------------
route map / milestone
1. CAN dyn create multiple spheres (other model)
2. create & prepare asset(*) automatic & By Go components info <eg: render, meshFilter>
3. create panel-gui : sim <dyn-change & Apply in runtime> for the orig-vulkan project!
4. support collision <multi - static & unity rigidbody, Collider>
5. support connections <ray, hit => create joint /connections > / pick interactive by mouse
5. support cut